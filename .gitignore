# Dependencies
node_modules/

# Next.js build output (never commit — regenerated by `next dev` / `next build`)
.next/
out/

# Env (keep secrets local)
.env*.local

# Debug / OS
npm-debug.log*
.DS_Store
